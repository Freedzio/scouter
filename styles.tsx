import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
	viewWrapper: {
		padding: 10,
		height: 100
	},
	textInput: {
		borderColor: 'black'
	}
});

export default globalStyles;
